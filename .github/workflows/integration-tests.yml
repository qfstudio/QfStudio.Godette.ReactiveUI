name: Integration Tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    - uses: actions/cache@v5
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: |
          ${{ runner.os }}-nuget-
    - uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'
    - name: Restore dependencies
      run: dotnet restore ./QfStudio.Godette.IntegrationTests.sln
    - name: Build
      run: dotnet build --no-restore ./QfStudio.Godette.IntegrationTests.sln
    - name: Test
      run: dotnet test --no-build --verbosity normal ./QfStudio.Godette.IntegrationTests.sln
